---
import BaseLayout from '../layouts/BaseLayout.astro';

// 遊戲列表 - 新增遊戲只需在這裡加一筆
const games = [
  {
    slug: 'ultimate-password',
    title: '終極密碼',
    description: '猜一個 1 到 100 的神秘數字，每次猜測都會縮小範圍，看你幾次能猜中！',
    emoji: '🔐',
    tags: ['猜數字', '經典'],
  },
  {
    slug: 'card-magic',
    title: '讀心撲克牌',
    description: '在心裡記住一張牌，我會把它移走。你信嗎？',
    emoji: '🃏',
    tags: ['魔術', '心理'],
  },
  {
    slug: 'symbol-magic',
    title: '符號讀心術',
    description: '在心中想一個兩位數，經過簡單計算後，我能猜中你對應的符號。',
    emoji: '🔮',
    tags: ['魔術', '數學'],
  },
  {
    slug: '21-cards',
    title: '21 張牌魔術',
    description: '從 21 張牌中選一張，只要告訴我它在哪一排，三次之後我就能找到它。',
    emoji: '🎴',
    tags: ['魔術', '撲克牌'],
  },
  {
    slug: 'example',
    title: '範例遊戲',
    description: '這是一個範例遊戲頁面，展示如何新增遊戲。',
    emoji: '🎯',
    tags: ['範例'],
  },
];
---

<BaseLayout title="首頁">
  <section class="hero">
    <h1>🎮 Henry 的小遊戲</h1>
    <p>一些我做的有趣小遊戲和互動實驗</p>
  </section>

  <section class="games-grid">
    {games.map((game) => (
      <a href={`/games/${game.slug}/`} class="game-card">
        <span class="game-emoji">{game.emoji}</span>
        <h2>{game.title}</h2>
        <p>{game.description}</p>
        <div class="tags">
          {game.tags.map((tag) => (
            <span class="tag">{tag}</span>
          ))}
        </div>
      </a>
    ))}
  </section>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: 3rem 0;
  }

  .hero h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }

  .hero p {
    color: #aaa;
    font-size: 1.125rem;
  }

  .games-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1.5rem;
    padding: 1rem 0;
  }

  .game-card {
    background: #1a1a2e;
    border: 1px solid #2a2a3a;
    border-radius: 12px;
    padding: 1.5rem;
    text-decoration: none;
    color: inherit;
    transition: transform 0.2s, border-color 0.2s;
  }

  .game-card:hover {
    transform: translateY(-4px);
    border-color: #4a4a6a;
  }

  .game-emoji {
    font-size: 2.5rem;
    display: block;
    margin-bottom: 0.75rem;
  }

  .game-card h2 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
    color: #fff;
  }

  .game-card p {
    color: #aaa;
    font-size: 0.9rem;
    line-height: 1.5;
  }

  .tags {
    margin-top: 0.75rem;
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .tag {
    background: #2a2a3a;
    padding: 0.25rem 0.625rem;
    border-radius: 999px;
    font-size: 0.75rem;
    color: #ccc;
  }
</style>
